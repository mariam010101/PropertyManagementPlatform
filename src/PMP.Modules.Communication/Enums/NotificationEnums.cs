namespace PMP.Modules.Communication.Enums;

public enum NotificationChannel
{
    InApp = 0,
    Email = 1,
}

public enum NotificationDeliveryStatus
{
    Pending = 0,
    Sent = 1,
    Failed = 2,
}
